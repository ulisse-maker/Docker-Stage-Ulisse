server {
    charset utf-8;
    listen 80;
    server_name localhost;
    root /var/www/html;

    location  / {
        index index.html;
    }
    
    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
}